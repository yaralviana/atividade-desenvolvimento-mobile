import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'atividade-desenvolvimento-mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
